package com.jiss.app.display;

public class BufferRegion implements ScreenRegion<BufferContext> {
    @Override
    public void draw(BufferContext context) {
        // Draw buffer using context.buffer, etc.
    }
}
