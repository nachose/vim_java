package com.jiss.app.display;

public class StatusLineRegion implements ScreenRegion<StatusLineContext> {
    @Override
    public void draw(StatusLineContext context) {
        // Draw status line using context.mode, context.screen, etc.
    }
}